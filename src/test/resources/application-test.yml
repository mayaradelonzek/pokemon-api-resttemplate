server:
  port: 8081
  error:
    include-message: always
  servlet:
    context-path: /rest

spring:
  datasource:
    url: jdbc:postgresql://localhost:3091/postgresTest
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop